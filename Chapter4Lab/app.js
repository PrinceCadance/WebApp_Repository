"use strict";

var main = function() { 
	window.alert("Hello world!");
}

$(document).ready(main); //jquery version of onload event 
//document ready when html is loaded, window.load means html AND images and files are ready
